name: Visual Regression Check

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  statuses: write

concurrency:
  group: visual-regression-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  visual-check:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      # ── Setup ──────────────────────────────────────────
      - name: Checkout PR
        uses: actions/checkout@v4

      # Buscar baselines da main (branch base) para comparar
      - name: Carregar baselines da main
        run: |
          git fetch origin main --depth=1
          # Sobrescreve baselines/ com a versão da main
          git checkout origin/main -- baselines/ 2>/dev/null || true
          if [ -d baselines ] && ls baselines/*.png 1>/dev/null 2>&1; then
            echo "has_baselines=true" >> "$GITHUB_ENV"
            echo "📁 Baselines encontradas:"
            ls -1 baselines/*.png
          else
            echo "has_baselines=false" >> "$GITHUB_ENV"
            echo "⚠️ Nenhuma baseline encontrada — first run"
            mkdir -p baselines
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      - name: Instalar Chromium (Playwright)
        run: npx playwright install chromium --with-deps

      # ── Captura + Comparação ───────────────────────────
      - name: Capturar screenshots
        run: npm run capture

      - name: Comparar com baselines
        id: compare
        run: |
          if [ "$has_baselines" = "false" ]; then
            echo "has_diffs=false" >> $GITHUB_OUTPUT
            echo "failed=0" >> $GITHUB_OUTPUT
            echo "✅ Sem baselines — nada para comparar, check aprovado"
            exit 0
          fi

          npm run compare || true

          if [ -f results/results.json ]; then
            FAILED=$(node -e "const r=JSON.parse(require('fs').readFileSync('results/results.json','utf-8')); console.log(r.summary.failed)")
            echo "failed=$FAILED" >> $GITHUB_OUTPUT
            if [ "$FAILED" -gt 0 ]; then
              echo "has_diffs=true" >> $GITHUB_OUTPUT
              echo "🔍 $FAILED diferenças visuais encontradas"
            else
              echo "has_diffs=false" >> $GITHUB_OUTPUT
              echo "✅ Nenhuma diferença visual"
            fi
          else
            echo "failed=0" >> $GITHUB_OUTPUT
            echo "has_diffs=false" >> $GITHUB_OUTPUT
          fi

      # ── Gerar meta.json ────────────────────────────────
      - name: Gerar meta.json
        run: |
          node -e "
          const fs = require('fs');
          fs.mkdirSync('results', { recursive: true });
          fs.writeFileSync('results/meta.json', JSON.stringify({
            commitSha: '${{ github.event.pull_request.head.sha }}',
            commitShort: '${{ github.event.pull_request.head.sha }}'.slice(0, 7),
            branch: '${{ github.head_ref }}',
            baseBranch: '${{ github.base_ref }}',
            prNumber: ${{ github.event.pull_request.number }},
            prTitle: process.env.PR_TITLE,
            actor: '${{ github.actor }}',
            runId: '${{ github.run_id }}',
            repository: '${{ github.repository }}',
            timestamp: new Date().toISOString(),
            hasDiffs: ${{ steps.compare.outputs.has_diffs || false }},
            failedCount: ${{ steps.compare.outputs.failed || 0 }}
          }, null, 2));
          "
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}

      # ── Status check ───────────────────────────────────
      - name: Status — pendente (diffs encontrados)
        if: steps.compare.outputs.has_diffs == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.payload.pull_request.head.sha,
              state: 'pending',
              description: 'Diferenças visuais encontradas — review necessário',
              context: 'visual-regression/review',
              target_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${{ github.run_id }}`
            });

      - name: Status — sucesso (sem diffs ou sem baselines)
        if: steps.compare.outputs.has_diffs != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.payload.pull_request.head.sha,
              state: 'success',
              description: 'Nenhuma diferença visual encontrada',
              context: 'visual-regression/review'
            });

      # ── Upload artefatos ───────────────────────────────
      - name: Upload resultados
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: visual-regression-results
          path: results/
          retention-days: 30

      # ── Comentar no PR ─────────────────────────────────
      - name: Comentar no PR
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const hasDiffs = '${{ steps.compare.outputs.has_diffs }}' === 'true';
            const hasBaselines = '${{ env.has_baselines }}' === 'true';
            const sha = '${{ github.event.pull_request.head.sha }}'.slice(0, 7);
            const branch = '${{ github.head_ref }}';
            const runUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${{ github.run_id }}`;

            let body = '';

            if (!hasBaselines) {
              body = `## ✅ Regressão Visual — Primeiro Run\n\n`;
              body += `**Commit:** \`${sha}\` · **Branch:** \`${branch}\`\n\n`;
              body += `Nenhuma baseline encontrada. Screenshots foram capturados.\n`;
              body += `Após o merge, as baselines serão criadas automaticamente.\n`;
            } else if (!fs.existsSync('results/results.json')) {
              body = `## ✅ Regressão Visual OK\n\n`;
              body += `**Commit:** \`${sha}\` · **Branch:** \`${branch}\`\n`;
            } else {
              const data = JSON.parse(fs.readFileSync('results/results.json', 'utf-8'));
              const s = data.summary;

              body = `## ${ hasDiffs ? '🔍 Review Visual Necessário' : '✅ Regressão Visual OK' }\n\n`;
              body += `**Commit:** \`${sha}\` · **Branch:** \`${branch}\`\n\n`;
              body += `| Técnica | Resultado |\n|---------|----------|\n`;

              for (const [tech, label] of [['pixel','Pixel'],['ssim','SSIM'],['region','Região']]) {
                const t = s.techniques[tech];
                const icon = t.failed > 0 ? '❌' : '✅';
                body += `| ${label} | ${icon} ${t.passed} OK / ${t.failed} FAIL |\n`;
              }

              if (hasDiffs) {
                body += `\n### ⏳ Merge bloqueado até review visual ser aprovado\n\n`;
                body += `**Para revisar:**\n`;
                body += `1. Baixe os artefatos do [workflow](${runUrl})\n`;
                body += `2. Extraia a pasta \`results/\` na raiz do projeto\n`;
                body += `3. Execute: \`npm run review\`\n`;
                body += `4. Acesse: http://localhost:3060\n`;
                body += `5. Ao aprovar, o status check será atualizado e o merge liberado\n`;
              }
            }

            body += `\n> 🤖 **PixelGuard** · [run \`${{ github.run_id }}\`](${runUrl})`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('Regressão Visual')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
